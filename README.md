# WRITI.AI - Guía de Inicio Rápido

Esta guía te ayudará a iniciar la aplicación localmente sin complicaciones.

## 🚀 Cómo Iniciar la App

Tienes dos formas de iniciar la aplicación:

### 1. Usando el Acceso Directo (Recomendado)
Simplemente haz doble clic en el archivo `iniciar_app.bat` que se encuentra en la carpeta raíz del proyecto.
- Esto abrirá una terminal y ejecutará el servidor automáticamente.
- Una vez veas que dice `READY`, puedes abrir tu navegador en: [http://localhost:3000](http://localhost:3000)

### 2. Manualmente via Terminal
Si prefieres usar la terminal:
1. Abre una terminal en la carpeta del proyecto.
2. Ejecuta el comando:
   ```bash
   npm run dev
   ```
3. Abre [http://localhost:3000](http://localhost:3000) en tu navegador.

## 🛠️ Requisitos
- Asegúrate de tener **Node.js** instalado.
- El archivo `.env.local` debe estar configurado con las credenciales de Supabase (ya debería estar listo).

## 📝 Notas
- Si la aplicación no carga, verifica que no haya otra aplicación usando el puerto 3000.
- Si necesitas ayuda adicional, ¡pregúntame!
